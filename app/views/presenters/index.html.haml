%h1 Listing presenters

%table
  %tr
    %th Name
    %th Bio
    %th Twitter handle
    %th Email
    %th Talk
    %th
    %th
    %th

  - @presenters.each do |presenter|
    %tr
      %td= presenter.name
      %td= presenter.bio
      %td= presenter.twitter_handle
      %td= presenter.email
      %td= presenter.talk_id
      %td= link_to 'Show', presenter
      %td= link_to 'Edit', edit_presenter_path(presenter)
      %td= link_to 'Destroy', presenter, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Presenter', new_presenter_path
